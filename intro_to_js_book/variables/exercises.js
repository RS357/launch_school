// 3) - error - foo is not defined. foo is scoped in a block that isn't exposed to the console.log statement 

// 4) - error - you cannot reassign a constant after defining it. 

// 5) "bar" - the foo definition in the block doesn't scope outside it - so the foo variable defined outside the block is outputted. 

// 6) No - because the CONST variables are declared in seperate blocks. 